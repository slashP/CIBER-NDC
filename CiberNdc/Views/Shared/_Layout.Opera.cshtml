<!DOCTYPE html> 
<html> 
    <head> 
        <meta charset="utf-8" />
        <title>@ViewBag.Title</title> 
        <meta name="viewport" content="width=device-width" />
        <link rel="stylesheet" href="@Url.Content("~/Content/jquery.mobile-1.0b3.min.css")" />
        <link rel="stylesheet" href="@Url.Content("~/Content/Site.Mobile.css")" />
        @ViewBag.Scripts
    </head> 
    <body>
        <div data-role="page" data-theme="a" data-content-theme="a">
            <div data-role="header"> 
                <h1>@ViewBag.Title</h1> 
                <div data-role="navbar">
                    <ul>
                        <li>@Html.ActionLink("Home", "Index", "Home")</li>
                        <li> @Html.ActionLink("Take Photo", "TakePhoto", "Home")</li>
                        <li> @Html.ActionLink("Upload Photo", "UploadPhoto", "Home")</li>
                    </ul>
                </div>
            </div>
            @RenderBody()
        </div>

    </body>
</html>